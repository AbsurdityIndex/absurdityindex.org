---
interface Props {
  billNumber: string;
}

const { billNumber } = Astro.props;
---

<span class="inline-block font-mono text-xs font-semibold bg-navy-800 text-gold-400 px-2.5 py-0.5 rounded-full tracking-wide">
  {billNumber}
</span>
